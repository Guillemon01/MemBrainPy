@model<test2>

def main() {
    /* Estructura: membrana “1” (piel) con una hija “2” */
    @mu = [[]'2]'1;

    /* Recursos iniciales */
    @ms(1) = a*3, b;
    @ms(2) = c*2;

    /* Reglas en 1 y en 2 */
    [a b --> d] '1';
    [c --> c (out)] '2';
}
